from setuptools import setup, find_packages

setup(
    name="prism",
    version="0.1.0",
    description="PRISM — Predictive Regime Intelligence & Signal Model",
    packages=find_packages(),
    python_requires=">=3.10",
)
